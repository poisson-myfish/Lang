module Main where
import Lexer
import Parser

main :: IO ()
main = do
  file <- readFile "test.lang"
  tokens <- return (tokenize (file ++ " "))
  exprs <- return (parse tokens)
  putStrLn $ show exprs
